name: "hello world"

on:
    push:
        branches:
            - master
            - production


jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: "print hell world"
            run: echo "print hello world"
            